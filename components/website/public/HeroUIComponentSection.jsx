"use client";

import React from 'react';

// Legacy V2 component — GrapesJS now handles all page rendering.
// Kept as a stub to avoid import errors in existing code paths.

export const CUSTOMIZATION_FIELDS = [];

const HeroUIComponentSection = () => null;

export default HeroUIComponentSection;
